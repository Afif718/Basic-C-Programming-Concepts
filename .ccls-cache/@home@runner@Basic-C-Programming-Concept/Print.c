#include <stdio.h>

// here is main is a buil-in function, without it program can't run

int mainn() {

  // here printf() is a build-in function
  // to use a build-in function we need to add the header file
  // for printf() function, header file is in <stdio.h>
  // so we need to add the header file to get the output


    //to get output in new line use \n
    //to get tab use \t 
    //to comment use \\
    
    printf("Hellooo Afif\n");
    printf("Hello Modhu!!!\n");
    printf("Hello Anish!!!\n");
    printf("Hello UCSI\nHello Dr Javed\t\t\t");
    
    return 0;
}