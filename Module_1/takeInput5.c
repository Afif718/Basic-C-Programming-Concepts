#include<stdio.h>
int main(){

    //Example of how to take a % as an input
    //variable declaration
    // int age, salary;
    // char d;
    
    // //to take input we need to use scanf function
    // scanf("%d%c %d%c", &age,&d, &salary, &d);

    // //print the input below
    // printf("%d%c %d%c", age,d, salary,d);
    // return 0;

    //another way to add a character with the input
    int a, b;

    scanf("%da %db", &a, &b);
    printf("%da %db", a, b);
    printf("\n");

    return 0;
}